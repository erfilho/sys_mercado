{{!-- <div class="card col-10 col-sm-5 col-lg-3 m-3 gap-3">
  <div class="card-body d-flex flex-column  justify-content-center text-start gap-2">
    <h5 class="card-title ">{{title}}</h5>
    <p class="card-text">{{body}}</p>
    <a href="#{{link}}" class="btn btn-primary ">Ver detalhes</a>
    <a href="/users/login" class="btn btn-primary ">Aproveitar agora!</a>

  </div>
</div> --}}

<div class="card col-10 col-sm-5 col-lg-3 gap-3 ">
  <div class="card-header">
    <h5 class="card-title">{{title}}</h5>
  </div>
  <div class="card-body d-flex flex-column justify-content-center text-start gap-2">
    <h6 class="card-subtitle mb-2 text-muted">{{body}}</h6>
    <a href="{{link}}" class="btn btn-primary" id="button">Quero este!</a>
  </div>
</div>