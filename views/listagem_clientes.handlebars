{{#if auth}}
  <nav id="navbar" class="navbar bg-purple-200 px-3 mb-1 w-100">
    <a class="navbar-brand" href="#">SysMercado</a>
    <ul id="main-navbar" class="nav nav-pills">
      <li class="nav-item">
        <a class="nav-link disabled" href="#"> {{usuario.login}} </a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#"> Clientes Cadastrados </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/login">Logout</a>
      </li>
    </ul>
  </nav>
  <div
    class="container-fluid w-100 d-flex flex-column justify-content-center align-items-center align-content-center"
  >
    <div class="container-fluid w-75 d-flex flex-column">
      <div class="w-100 text-center">
        <h1 class="fs-4">Clientes cadastrados</h1>
      </div>
      <div>
        <button class="btn btn-primary" onclick="verCad(0)">Cadastrar novo cliente</button>
      </div>
      <div class="d-flex flex-column gap-2">
        {{#each clientes}}
          {{> cliente}}
        {{/each}}
      </div>
    </div>
  </div>
{{else}}
  <p> Login please </p>
{{/if}}